<?xml version="1.0" encoding="UTF-8"?>
<!-- Configuration schema for astrowidget. -->
<!-- The KConfig XT framework reads this to build a settings backend; -->
<!-- the values are exposed to QML as `plasmoid.configuration.<key>`. -->
<kcfg xmlns="http://www.kde.org/standards/kcfg/1.0"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:schemaLocation="http://www.kde.org/standards/kcfg/1.0
                          http://www.kde.org/standards/kcfg/1.0/kcfg.xsd">
	<kcfgfile name=""/>
	<group name="General">
		<!-- Night-vision red mode: desaturates the palette to reds to
		     preserve dark adaptation if the widget is open near a scope. -->
		<entry name="nightVisionMode" type="Bool">
			<default>false</default>
		</entry>
		<!-- Which night tab is selected by default when opening the popup.
		     0 = Tonight, 1 = +1 night, 2 = +2 nights. -->
		<entry name="defaultNightTab" type="Int">
			<default>0</default>
			<min>0</min>
			<max>2</max>
		</entry>
		<!-- Dismissed degradation warnings, as a JSON array of "<siteId>|<code>"
		     strings (e.g. ["udro|http_403"]). Written when the user clicks
		     "Don't show this again" on a site's Astrospheric-failure notice and
		     read back to keep that exact site+reason hidden. Stored as a string
		     because KConfig has no list type that round-trips cleanly to QML. -->
		<entry name="dismissedWarnings" type="String">
			<default>[]</default>
		</entry>
	</group>
</kcfg>

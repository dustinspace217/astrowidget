[pytest]
# Registered markers (unknown marks only warn, but registration keeps intent
# explicit and greppable).
markers =
    real_aod: test exercises the REAL fetch_open_meteo_air_quality with its own requests-level mocking — the autouse AOD network stub in tests/fetcher/conftest.py opts out for it (QA 2026-06-09)
